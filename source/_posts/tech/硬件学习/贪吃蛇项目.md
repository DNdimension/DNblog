---
title: 贪吃蛇项目
categories:
  - tech
  - 硬件学习
date: 2026-06-29 13:48:03
tags:
---
# 26.06.29
## 上午  
回来研究这个沟槽的点阵屏，不仅右半边发光不能持续，点亮顺序也是依托。  
<figure style="text-align: center; margin: 20px 0;">
  <img src="https://s3.bmp.ovh/2026/06/29/O4nw8g8W.webp" style="width: 45%;">
  <figcaption style="color: #888; font-size: 14px; margin-top: 8px;">
    右半屏无法持续点亮
  </figcaption>
</figure>  

要在这个点阵屏上渲染一些东西，用坐标控制比较直观。于是先写了一个ptoi，把pos(x,y)对应到像素索引i。  
但是右半边屏幕的点亮问题还是很难解决。正常点阵在setPixel之后运行一次WriteScreen就能常亮，但右半边必须循环WriteScreen才能保持能看的亮度。灵光一现发现只要尽量减少阻塞命令的执行，把loop()大部分时间都用于WriteScreen就能有不错的观感。这样就不能用常用的delay()命令，因为这会阻塞全局loop的执行。此时联想到了在mc sapi开发时设想过的一种设计，即维持一个时刻变量，使之周期性均匀变化，在特定的值时执行特定函数。  
{% codeblock cpp lang:cpp %}
uint8_t dnt;
setup() {
  dnt=0;
  ...
}
loop() {
  if(!dnt) {
    dnt=DNT;
    CLRPIXEL();
    SETPIXEL(); 
  }
  else dnt--; //dnt为0时构图并重置
  WRITESCREEN(); //绝大部分时间渲染
}
{% endcodeblock %}
先写了一个二维弹球试手  
<img src="https://s3.bmp.ovh/2026/06/29/fABPjmbS.webp" width="45%">  


## 下午  
考虑一会决定写一个贪吃蛇项目，算是目前有难度但不高能写的。  
使用joystick摇杆模块控制方向，发现原来还有个Z轴。
先把长度为一时的移动给写了，又写了个边界判断。写完边界判断就想写失败界面，提示"OVER"，于是花了半天扣字模  
蛇身的移动需要使用队列，自行模拟由于不知道操作次数所以比较难搞，在开发板上又不能使用含有动态内存分配的std::queue，最后下了一个第三方库ringbuffer，需要指定缓冲区大小。设定为uint8_t类型，单个像素占用8B，考虑到蛇身长度为15就差不多可以结束了，于是分配120B，不算太多  
HT1632库中没有提供检查一个像素是否点亮的，就自行修改了库，写了一个getPixel接口  

# 26.06.30  
## 上午  
把吃食物和蛇身长度大于1时移动的逻辑写了，接下来时随机生成食物，需要用到random()和randomSeed()  
randomSeed()在setup()中设定即可，seed可以使用悬空模拟读入引脚受电磁波干扰产生的信号，random(u)产生$[0,u-1]$的随机数  
食物生成写完之后写了自身碰撞的检测，后面又把胜利时显示WIN的字模扣出来了，代码基本完成，尝试优化  
尝试把失败和胜利的显示逻辑统一，使用了template，算是第一次用c++的现代语法  
{% codeblock cpp lang:cpp %}
template <size_t N>
void showInfo(const uint8_t (&arr)[N]) {
  ...
  for(size_t i=0;i<N;i++) matrix.setPixel(pgm_read_byte(&arr[i]));
}
{% endcodeblock %}
接下来把ptoi的公式进行简化，能算的先算了，*8改成<<3  
PROGMEM把OVER和WIN的字模存在FLASH而非SRAM  
本来还应该引入状态机，但考虑到这可能影响点阵屏右半屏的显示，决定就使用dnt  
最后程序占用5872B的存储，383B的内存  

<img src="https://pic1.imgdb.cn/item/6a436eda514f59159c63d6ca.jpg" width="45%">  


<img src="https://s3.bmp.ovh/2026/06/30/BZPsQKPD.webp" width="45%">  

